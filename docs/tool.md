# Tool 资源 - 工具管理

Tool 资源提供工具管理功能，包括列出工具 ID 和获取指定模型的可用工具列表。

## 📋 方法列表

1. [ids](#1-ids) - 获取所有可用的工具 ID 列表
2. [list](#2-list) - 获取指定提供商和模型的可用工具列表

---

## 📖 详细文档

### 1. ids

获取所有可用的工具 ID 列表。

包括内置工具和动态注册的工具。

**返回值:**
- `List[str]` - 工具 ID 列表

**示例:**
```python
tool_ids = client.tools.ids()
print(tool_ids)
# 输出: ['read', 'write', 'edit', 'bash', 'grep', ...]
```

---

### 2. list

获取指定提供商和模型的可用工具列表。

**参数:**
- `provider_id` (str) - 提供商 ID
- `model_id` (str) - 模型 ID

**返回值:**
- `List[Dict[str, Any]]` - 工具列表，每个工具包含：
  - `id` - 工具 ID
  - `description` - 工具描述
  - `parameters` - JSON Schema 参数定义

**示例:**
```python
tools = client.tools.list("anthropic", "claude-3-5-sonnet-20241022")
for tool in tools:
    print(f"{tool['id']}: {tool['description']}")
    print(f"参数: {tool['parameters']}")
```

---

## 💡 使用建议

1. **查看所有工具** - 使用 `ids()` 查看所有可用的工具
2. **查看模型工具** - 使用 `list()` 查看特定模型支持的工具

## 🔗 相关资源

- [MCP 资源](mcp.md) - 模型上下文协议
- [Session 资源](session.md) - 会话管理
